# Burning Meadow — Roadmap to 1.0
**The master production plan.** Grounded in a full read-only audit of the live build (content, art, audio, systems, release, legal), 2026-06.
Companion doc: [`CONQUEST_REDESIGN.md`](CONQUEST_REDESIGN.md) — the "Successor Wars" redesign, treated here as one (large, optional) workstream.

---

## Status — live progress log

**As of this planning session:**
- ✅ **M0 (ship-hygiene) COMPLETE** — debug prints gated behind `DEBUG_PACING` (Combat.gd); CC-BY/license attributions fixed in the in-game Credits + `CREDITS.md`; non-PD **Beksiński** PD-claim removed; `scripts/_shot.gd` deleted; 3 blank cards fixed; `README.md` rewritten; CLAUDE.md counts corrected. All parse-checked clean. (Blockers **B1–B8** in §2 are done.)
- 🎨 **Open art follow-ups** (not blocking; fit the one-at-a-time PD/painterly pipeline): (1) visually confirm no card portrait is a non-PD work (Beksiński *credit* removed, *art* unverified); (2) give `plague_bell` its own painterly icon (currently borrows `mass_grave`'s); (3) replace corrupt `assets/creatures/pikeman.png` (masked by a `shieldbearer` alias).
- ✅ **RESOLVED 2026-06-10 — the user picked PATH B** (build the Successor Wars redesign into 1.0; see the dated entry below). Meta-progression scope and platform targets remain open — decide by M2 at the latest.
- 🧭 **Signal (2026-06-10):** unprompted, the user described wanting (a) routes where the map/terrain mechanically matters, (b) fighting **the lords you didn't pick** as bosses, (c) **card picking outside combat, Inscryption-style** — (b) and (c) are exactly CONQUEST Phases 1/3/4; (a) is now specced as **CONQUEST Phase 2.5 (terrain-weighted routes)** + Open Decisions #8 (free vs paid recruit) and #9 (terrain weight depth). The user is converging on the redesign vision — but the formal Path A/B/C pick has still not been made.
- 🛠️ **2026-06-10 session (verified by screenshot; committed in `5417d32`):** three combat-legibility fixes — battlefield stat orbs 38px with 18px white outlined numerals (`Card2D._build_compact_layout`), enemy intent badges 11px label → 15px dark pill (`Combat._update_intent_display`), relic chips: tier halo 0.55→0.30 + painted icons overscanned 16% to crop their baked canvas glow (`GameTheme.make_relic_chip`).
- 🗺️ **Strategy-map mockup v6 — REAL GEOGRAPHY (Sicily) + carved roads (PORTED LIVE — see next entry):** v5's A/B experiment proved **node count is the legibility dial** (38-site lattice = nonsense; **11–15 sites = campaign map** — validates CONQUEST Phase-2 act sizing with pixels; probe generates its own 8-row act, **game constants untouched**). v6 (user-directed, same session) swaps the procedural island for **real local geography**: hand-traced Sicily coastline (lon/lat → px), **Etna as the boss seat** (cone + glowing caldera + lava threads + smoke plume; keep at its foot; scorch = lava country), Strait of Messina + mainland sliver, Aeolian/Egadi isles, anchored massifs (north-coast chain carve-shielded so roads read as passes). **Roads are carved INTO the map** (incised groove: NW rim shadow / SE lit rim / dark channel / packed-earth floor / state tint riding in the cut). Antique-chart furniture per reference research (CK3 paper-map dev diaries, real chart conventions): **waterline rings** fading seaward, sparse wave ticks, harbour marks (Salt Haven / North Haven / The Old City), cartouche double-rule + finials, sea labels (The Upper/Middle Sea, The Narrow Strait, The Mainland), title "ACT I · THE BURNING ISLE". Node placement: march spine camp→Etna with min-spacing relaxation (46px) + island polygon clamp. **v6.1 detail pass** (research: Lehmann hachures + chart conventions): finer mesh (CELL 20), hachure slope strokes, tilled-field furrows along roads, ocean depth-banding + shoal stipple + water-only graticule, double-ruled coast, denser shadowed forests, lava-crack fissures, river tributaries, harbour town clusters. v5 procedural renderer preserved at `tools/screenshot/MapProto_v5_procedural.gd.bak`. Renders: `map_proto.png` (fresh) / `map_proto_midrun.png`; v4/v5 evidence chain kept. Probe overwrites save slot 0 (`run_N.save.bak_20260610` backups exist).

- ✅ **2026-06-10 (later session): Sicily map PORTED LIVE — explicit user go on this piece of the redesign.** The user approved v6 and directed the port ("porting the Sicily map into the live game"); **the act shrink ships with it** because the validated look requires 11–15 sites. This is a **partial, user-approved pull-forward of CONQUEST Phase 2 (act sizing) + §12 (territorial map)** — the formal Path A/B/C pick is *still* open for everything else. What landed (committed in `5417d32` on `feature/visual-polish-and-content`, with `SAVE_VERSION` bumped to 2 to retire pre-shrink saves and an act-3 render added to the probe): `scripts/scenes/MapTerrain.gd` (new base class — the full v6.1 Sicily plate: coast/massifs/Etna keep, carved roads, political layer, antique-chart furniture, per-act dressing); `scripts/scenes/MapView.gd` rewritten to extend it (site hover/click buttons + tooltips with encounter/mutator intel, node→scene flow, top HUD, deck viewer, meta-relic pickers, save checkpoint — old parchment chart preserved at `tools/screenshot/MapView_parchment.gd.bak`); `scenes/map.tscn` slimmed to script-only Control (no Background — the plate IS the background); `RunState.gd` map constants 15→**8 rows** (`BOSS_ROW 7`, `REST_ROW 6`, `NUM_PATHS 3`, `MIN_ELITE_ROW 3`) + **acceptance loop enforcing 11–15 sites/act** (deterministic per-act sub-seeds so attempt count doesn't shift later acts) + 3× path-merge bias (trunk-road look). **Verified by rendering the real scene** in three states via `tools/screenshot/_probe_map_live.gd` (fresh act-1 / 3 marches in / act-2): provinces counter ticks 0/13→3/13, claimed wash + pennants render, act 2 re-dresses (browner plate, The Collector boss) and resets the counter. Fix found by the probe: right HUD (potions/deck) was hardcoded at x=1640 — **off-canvas under `canvas_items` stretch (coordinate space = 1600×900 project viewport, not window pixels)**; now anchored off `size.x`. Renders: `map_live_fresh.png` / `map_live.png` / `map_live_act2.png`.

- ✨ **2026-06-10 (later): campaign-map polish pass** (verified by 5-state probe renders incl. a mid-march frame): (1) **animated overlay** (`MapView.MapPulseOverlay`) — breathing amber rings on reachable sites (crimson ring on the keep when the boss road opens), a **distinct army standard** (swallow-tail banner, ground ring, gentle sway) at the player's position replacing the old static pennant that was near-identical to conquered-site pennants, Etna ember drift, and a **0.55s commit march**: the standard walks the carved road to the chosen site before the scene fade. The 25k-primitive plate stays static; the overlay redraws ~a dozen primitives per frame. (2) **Parchment tooltips** (`MapTipButton` + `_build_map_tooltip`: gilt name line + dim body on dark ink) replacing the stock gray box on site/potion/deck buttons. (3) **Per-act cartouche subtitle** — THE FIRST / SECOND / LAST MARCH — so the three sieges of the keep read as three campaigns. (4) **Legend** now lists only site types present on the act, seated on a faint band. (5) Bugfix: treasure sites rendered an icon-less chip (`_node_icon` was missing the `tex_node_treasure` case). Remaining map ideas parked: per-act camp/keep anchor sweep (real fix for the same-keep problem, CONQUEST-adjacent), controller-friendly site intel (tooltips are hover-only).
- 🏰 **2026-06-10 (later): per-act campaign legs — the same-keep problem is fixed.** The war now sweeps the island one leg per act: **west landing → a keep in the northern passes (act 1) → down through the southern grain country (act 2) → up into the lava country at Etna's foot (act 3)**, with each act's camp pitched exactly where the last keep fell (`keep_lls` anchors in `MapTerrain._read_run_map`, keeps clamped 34px inland so the boss pin can't land in surf). The march spine is now a camp→keep **vector** (legs aren't pure west→east): sites advance along it, lanes fan perpendicular via `_perp_extent` (replaces `_island_y_span`), along-spine jitter scales with the row step so rows can't visually reorder on short legs. Two couplings found and fixed: **scorch was centered on `_boss_pos`** — under the sweep it would have burned a mid-island keep while Etna sat clean; now centered on `_etna_peak` (still grows 175→205→235 by act, so the burn spreads as you approach it). And **provinces now cap at ~240px** from the nearest site, so the political layer hugs the act's corridor and the rest of the island reads as wilds (`_draw_political` already skipped `-1` cells). Verified by 5-state renders: act 2's "YOUR CAMP" sits exactly on act 1's keep site; act 3 marches into the scorch. Etna is visible as looming geography in every act and only becomes the destination in the last.

- 🎬 **2026-06-10 (later): run-loop coherence pass** (audit by rendering all five in-run screens via `_probe_scene`, verified by re-render): (1) **Quick encounter intro for normal fights** — the boss/elite intro banner (`_show_encounter_intro`) gained a `quick` variant (smaller name, no type prefix, lighter dim, 0.8s hold, normal-volume cue) that fires on normal combats **only when there's something to announce** (fight passive or mutator); vanilla skirmishes still start instantly. Directly attacks "fights feel samey": the one line that makes this fight different is now read before it fires. (2) **Rest banners restyled to the chart language** — `GameTheme.make_choice_banner` was the last UI-kit holdout (accent-flooded white-glyph icons read as flat green-heart/yellow-puzzle toys); now dark-ink panel + tan rule, parchment-tinted sigil icons with drop shadow, gilt titles, accent kept to a short under-title rule + the hotspot glow. REST icon switched to the map's campfire glyph (same symbol everywhere). (3) **Kindling art fixed** — `assets/creatures/kindling.png` was a truncated/corrupt export (top quarter campfire, rest black; same disease as `pikeman.png`); replaced with the complete `kindling_alt` painting. NOTE: original Midjourney gouache sources for both kindling AND the corrupt pikeman exist locally (surfaced by the import scan) — the pikeman follow-up (§ art) can be fixed the same way. Audit verdicts on the rest: Shop/Event/GameOver pass (Shop's colored inspect-panel lines are the CARD_TRIBES accent system — deliberate card language, left alone).

- 🧰 **2026-06-10 (later): product-shell + data pass** (all verified): (1) **Pikeman art FIXED** (closes art follow-up #3) — the staging gouache source was *itself* truncated (same disease as the old file), but `PIL ImageFile.LOAD_TRUNCATED_IMAGES` salvaged 95% of it; cropped the dead 47 rows, saved clean 1344×844, removed the `pikeman→shieldbearer` mask alias (`veteran→pikeman` now resolves to real art too; verified both cards render). (2) **Keep names** — the act keeps are now places: THE PASS GATE / THE GRANARY KEEP / THE CINDER SEAT on the map plaque above the boss name (common-noun lore rule; verified all 3 acts). (3) **Balance telemetry** — `RunState._append_run_log` writes one CSV row per finished run to `user://runs.csv` (timestamp, result, hero, ascension, seed, act, floor, hp, gold, fights, deck size, relics, cause of death; comma-escaped). Local only. Verified, then test rows deleted and `meta.save` hand-restored (the test victory had polluted `fastest_victory_floors`/`unlocked_ascension` — **warning: any probe calling `end_run` writes real meta**). (4) **Release shell started** — project icon (`res://icon.png`, kindling creature crop) + boot splash (`main_menu.png` over ink, seamless into the menu) wired in project.godot; export presets remain (they're gitignored/machine-local by design). (5) **Colorblind check PASSES** — deuteranopia simulation (Machado matrix) on the campaign map: amber-vs-crimson stays separable by *luminance* (bright gold vs dark brown) and the shape hierarchy (pennant / ringed chip / dim chip) is redundant signaling; no palette toggle needed. Evidence: `tools/screenshot/_cb_sim.png`.

- 🏛️ **2026-06-10 (later): PATH B PICKED — the redesign ships in 1.0.** The user chose Path B explicitly ("ah yea i want that for sure"). Sequence: M0 ✓ → trimmed M1 → **M3 → M4** → M2 → M5 → launch. A pre-build suggestion pass was delivered covering (a) collisions between the spec and the live build (faction engines must fire by round 2–3 in normal holds — fights snowball fast; per-faction **reinforcement wave schedules** should replace the uniform 1/round drip; engine **legibility hooks** ride the live intent/threat/mood systems; the Sicily plate already answers Open Decision #6 — provinces become kingdoms, the per-act leg flies to the chosen rival's seat), (b) recommended answers to all 9 `CONQUEST_REDESIGN.md` §13 open decisions, (c) a build order starting with the faction worksheet → Phase 0–1 scaffolding → a **one-matchup vertical slice** before any content bulk. **The full pass is recorded as `CONQUEST_REDESIGN.md` §15** (15.1 recommended calls / 15.2 authoring rules / 15.3 amended build order). **Building has NOT started** — get the user's go on the §15.1 calls (ending #1 and rival order #4 are the most taste-driven), then execute §15.3.

- 🏗️ **2026-06-10 (later): CONQUEST BUILD STARTED — the §15.3 vertical slice is LIVE** (user go: "continue this"; §15.1 calls taken as recommended). Four commits, every step probe-verified (84-check headless suite `tools/_probe_conquest_scaffold.gd` + 6 render-verified screens `tools/screenshot/conquest_*.png`):
  - `1e194a9` **Phase 0–1**: faction tags on all 40 encounters (worksheet consumed verbatim) + `get_ids_for` faction filter; HeroDB `FACTIONS` table (names locked, banner colors, engine lines, lord titles); RunState rival deal (seeded from run_seed; player-chosen order deferred per §15.1 #4), `act_faction`, `holds_broken_in_act`, **SAVE_VERSION 2→3**; `runs.csv` grows rival_act1-3/finale_rival/holds_broken columns (old-schema file shelves itself).
  - `0dd498a` **Kingdoms + rival lords + boss gate**: acts are faction-pure kingdoms (`EncounterDB.get_kingdom_pool` — cross-act borrow for thin factions, band-norm HP rescale + ±1/±1 stat rails, old bosses demote to capital-stronghold elites, top-up keeps un-authored factions playable); **THE STALWART** rival kit (Formation: `formation_drill`/`formation_lockstep`, phase thresholds ride the act ratio) with faction stand-in bosses covering the other four rivals; boss gate — keep visible from the start, road opens at 2 broken holds, `get_available_nodes` is the single gate source, generation guarantees every route carries ≥2 fights (memoized DFS in the acceptance loop).
  - `2e53313` **Recruit camps + decoupling**: 1–2 guaranteed muster camps per kingdom (converted pre-validation so the gate guarantee holds); **THE MUSTER** screen — free 1-of-3 draft (Inscryption model), offer leans to the invaded kingdom via a keyword-affinity table; fights pay gold inline (40/60/55) and normal fights skip Reward entirely; card-offer relics (Scout's Emblem / Busted Crown / Collector's Tome / Stardust Vial) ported to the muster; Reward slimmed to the elite/boss relic pick.
  - `e90ef18` **The throne**: after the act-3 rival falls, Reward routes into the amalgam finale instead of ending the run — **THE ACOLYTE ASCENDANT** (40 HP, 3 phases channeling the burned kingdoms: Swift husk trickle → formation_drill → doom_bell), `finale_stage` persists and MapView redirects throne-door resumes into the fight; spared rivals without an authored amalgam end at act 3 as before; eternal-cycle victory line per §15.1 #1.
  - **Remaining for the full redesign:** 4 rival kits + 4 amalgams; player-chosen rival order UI at act transitions; the ~8–12 faction gap encounters (lanternhall front-load) + re-levels; per-faction reinforcement wave schedules + engine legibility hooks (§15.2); `overrun`/`formation` as player-facing keywords; map faction-skinning (rival banner/political wash off `FACTIONS.color`); matchup-matrix tuning fed by the new telemetry columns.

- ⚔️ **2026-06-10 (final sprint, 4 parallel agents + integrator): six of the seven "Remaining" items above LANDED** — 7 commits, each parse-checked; conquest probe now **85/85 PASS**:
  - `3fc8b62` **Player-chosen rival order** — after act-1/2 boss spoils, Reward shows **THE NEXT MARCH** (chart-banner pick of the remaining lords; `RunState.choose_next_rival` swaps the deal late-bound, no map regen). Skipped when <2 rivals remain / legacy / finale.
  - `b63d60b` **Map faction-skinning** — unclaimed provinces wear the rival's desaturated banner dye (alpha 0.13 under the claimed amber 0.22, so conquest visibly eats it); keep pennant/plaque dyed; cartouche reads `…AGAINST THE <KINGDOM>`. Empty-deal fallback pixel-identical. **Render-verified** (`tools/screenshot/faction_map_act1/_mid.png`); act-2/3 + legacy renders still owed (probe wrote 2 of 5 states).
  - `64cbe06` **Overrun + Formation player-facing** — start-of-round pass in KeywordEffects (after Doom, round 2+, side-agnostic): Overrun = +N ATK this round into an open opposing front; Formation = permanent +1/+1 beside a rowmate (mirrors the Stalwart wall). Glass Knight/Doom Knight/Paladin/Iron Bastion carry them; muster AFFINITY routes them to grasswake/last_wall.
  - `457227e` **Per-faction reinforcement waves + telegraphs** (the last "samey" lever) — `FACTION_WAVE_SCHEDULES` on tagged normal holds: Grasswake 2,2,0… / Last Wall 1/round +1 HP bodies / Owed re-raise ≤2 banked deaths / Lanternhall every other round / Everflame 0,1,2 + half-broken surge ("THE FUSE CATCHES"). Next-wave chip under the incoming-damage chip (doubles as the Tithe corpse-counter), flood info line, per-faction combat moods, quick intro names the engine line. Untagged/elite/boss/rival keep the live drip exactly; round-8 anti-stall escalation still wins.
  - `9d75031` **All 4 missing rival kits + all 4 missing amalgams** — THE RAIDER (Swift face-chip wake), THE ACOLYTE (Altar tithe w/ visible Charges, sacrifice tax reactive), THE PYROMANCER (top-deck exile, keyword theft), THE KINDLER (board-wide burn his bodies shrug, Doom Cinders); amalgams for every spared lord channel the burned kingdoms in 3 phases. Data-only — every passive_id verified against live handlers. **Plus a latent live bug fixed:** `make_card_data` dropped deck-authored `doom`/`doom_damage`, so Bellringer/powderkeg Cinders detonated on first tick. Probe finale check flipped positive (throne always opens) with the graceful missing-kit branch kept via a fabricated rival.
  - `35667a6` **Art aliases** for all 11 new kit creature names (precedent-matched: Altar→blood_pyre, Pyre Tender→torchbearer, …). `7c10ea6` baseline (this entry's predecessor + Reward relic-row fix).
  - **Still remaining:** faction gap encounters (lanternhall front-load) + re-levels; matchup-matrix tuning off telemetry; render-verify map act-2/3 + legacy states and a live-fight pass over waves/keywords/kits (all probe/static-verified only so far); `Recruit.gd` AFFINITY could eventually read faction tags directly.

- 🏺 **2026-06-10 (later): HISTORY PASS — the five kingdoms now channel Sicily's real ages** (texture only: ids, stats, mechanics, saves untouched; parse-checked clean; conquest probe 85/85). The map is already Sicily, so each rival lord reads as one age of the island's actual history: Grasswake = the **horse-conquest** (landless brothers over the water, forty lances), Last Wall = the **legion** (drill, line-relief, the eagle), Owed = the **merchant-god city** (the furnace altar, the ledger), Lanternhall = the **geometer's city** (burning mirrors, the harbor claw, circles), Everflame = the **liquid fire** (the sealed recipe, fire that burns on water). What changed:
  - **HeroDB.FACTIONS** — elements re-keyed (Storm→Horse & Salt, Rot→Ash & Coin, Frost & Star→Mirror & Star), lord titles personalized: The Landless Brother / The Last Centurion / The Keeper of Ledgers / The Measurer of Circles / The Keeper of the Recipe.
  - **Kit + amalgam rosters renamed** where the old element lingered (same statlines; same art via new `CardArtAliases` keys; tribes added to `GameTheme.CARD_TRIBES`): Wind Harpy→Salt Harpy, Grass Runner→Wake Hound, Sky Stalker→Pennon Griffin, Storm Chief→First Ashore · Standard Bearer→Eagle-Bearer, Captain→Centurion, Iron Vanguard→First Spear · Skeleton→Debtor, Bone Walker→Collateral, Dark Priest→Furnace Priest, Gravewarden→Vault Warden, Risen→Repossessed, Altar→**Furnace Altar** (Combat's altar tick matches both names; the Owed's 3-Charge champion is now **Paid in Full** with its own "THE LEDGER BALANCES" banner — legacy Cultist Enclave unchanged) · Frost Adept→Claw Adept, Glass Sniper→Burning Glass, Null Beast→Ink Beast, Frost Lector→Cold Lector · Hellfire Imp→Siphon Imp, Coal Hulk→Pitch Hulk. Renames are scoped to the lords' kits/amalgams/phase-summons — gap encounters keep the generic bestiary on purpose (household troops are the named elite; the same statline under a faction name is the tell you're fighting the lord's own).
  - **Preambles** now carry each age's signature image; **wave captions** sharpened (last_wall "FRESH RANKS — TOUGHER", lanternhall "A MIRROR FLASHES", Everflame surge "FIRE ON THE WATER" + matching info toast).
  - **Rival lords got authored voices** — `PRESENCE_BARKS` rival_* entries (amalgams reuse them via an `amalgam_`→`rival_` fallback in `_pick_bark`); the Pyromancer's slay line is "Do not disturb my circles."
  - Alongside in the same tree (separate prior-session fix, riding the same commit): Summon-keyword dispatch consolidated into `KeywordEffects.dispatch_on_enter` (single dispatch point; keyword read before transform on-enters can graft it; Combat's duplicate summon pass removed).

- 🗺️ **2026-06-11: campaign-map navigation + territory legibility + route balance** (render-verified: `map_live_zoom`/`map_live_march` + refreshed baselines; balance counted by `tools/_probe_map_balance.gd`):
  - **The chart zooms and pans.** Wheel zooms toward the cursor (1.0–2.4×), drag pans while zoomed, and the map *opens* eased onto the army standard (0.55s to 1.45×) — returning from a room reads as picking the chart back up where you stand; wheel out for the whole island. Implementation: every plate layer draws through a `draw_set_transform` view transform in `MapTerrain._draw` (the cartouche/legend UI resets to identity and stays screen-fixed); MapView re-seats its invisible site hit-buttons on every view change (probe asserts 0.00 px seat drift); `MapPulseOverlay` rides the same transform so rings/standard/the commit march stay glued at any zoom. The sandbox (`map_proto.tscn`) inherits zoom for free.
  - **Map perf RESTRUCTURED (the user's "still lagging" follow-up; 2026-06-11 later session).** Three structural layers in `MapTerrain`: (1) the whole plate paints on **`PlateItem`**, a child canvas item that re-records only on content changes — zoom/pan just set its `position`/`scale`, so view changes do zero paint work (all `_draw_*` plate functions now take a target CanvasItem `tgt`); (2) **two offscreen 2× clone bakes** (`bake_mode` = "geo"/"plate") collapse the plate to ONE textured quad — geography (~22k prims, act-constant) bakes once per act, the complete plate (geo quad + campaign ink) re-bakes per open since ink follows claims, and a few thousand live AA ink commands otherwise cost ~12ms of GPU replay *every frame*; (3) **per-act cache** in `RunState.map_plate_cache` (mesh arrays + geo texture, cleared on new run/load/act change) so re-opens skip generation entirely. MapView gates the opening focus ease on `plate_baked` so it never plays over heavy frames. Probe-measured: view-change **132.5 → 3.5 → 2-3.5 ms/frame** across the three architectures; map re-open **~700ms every visit → ~120-150ms** (full gen only on the act's first open, ~750ms once); decorations now bake *under* the political wash (territory dye tints the rival's forests — reads as a realm, not stickers). GOTCHA for posterity: a **zero-size Control is culled whenever its canvas transform isn't identity** — the plate item must carry the real 1600×900 rect or it renders only at zoom 1.
  - **Territories read as the rival's realm.** Faction wash alpha 0.13→0.20 (and less desaturated), claimed amber 0.22→0.30, and the unconquered kingdom's interior province borders now carry his dye instead of neutral ink — conquest visibly eats his color site by site.
  - **The WHOLE island is politically carved (user follow-up: "shouldn't the non-taken territory have colors too").** Every Sicilian cell now belongs to one of the run's three rival realms — nearest-seat zones around the act keeps (`KEEP_LLS`, `_assign_zones`, mesh-cached) — and wears that lord's dye as a faint wash (0.15) with quiet ink borders between realms. Realms whose act is already won flip wholesale to soft gold (0.13), so by act 3 two-thirds of the chart glows conquered while the last realm holds the east. Kingdom names sit on their land in the live ink (`_draw_kingdom_names`: gold when taken, brightened dye when waiting; the active FRONT skips its land name — the cartouche carries it and its corridor is busy with chips). Legacy runs without a rival deal skip the overlay entirely. Render-verified across acts 1→3 (Everflame west → gold; Owed center → gold; Last Wall east holds to the end).
  - **Fewer fights, more musters** (user ask): muster camps 2→3 guaranteed per act (early/mid/late bands) and conversions eat COMBAT sites first (events only when a band has no fight); combat roll 41%→37% (events 22%→26%). Counted over 15 generated acts: **~4.2 fights + ~2.7 camps per act** (was ~5.5+ fights, ≤2 camps). The acceptance loop still guarantees every route carries `HOLDS_TO_OPEN_LORD` fights, so the keep gate can't starve.

- 🃏 **2026-06-11 (later): CARD FRAME v7 — "campaign document" — LIVE** (user: the cards "look kind of sus"). Diagnosis: material mismatch — glossy billiard-ball stat orbs, a cartoon ribbon nameplate, a flat dark panel, and the cel-shaded painted frame texture all fought the PD-master art AND the game's antique-chart identity (map plate / parchment tooltips / rest banners). v7 puts cards in that same material world: dark lacquered ink board, **per-rarity metal double rules** with diamond finials (bronze / silver / gold / pewter; curse = weathered bone), art mounted as a **gilt-filleted plate** with inner shadow, dark-ink nameplate with **gilt Cinzel name** (auto-shrinks for long names), **matte wax-seal stats** (sealing-wax navy cost / burnished-bronze ATK / oxblood HP — same 56px corner boxes as before, so the CardTextureCache bake-overlay contract is untouched; the overlay's ATK numeral flipped dark→cream for the darker seal), ink+gilt **keyword roundels**, parchment text well with a gilt hairline, and spells closing on a letterspaced gilt footer line. Battlefield tokens re-skinned to match (same 38px boxes + 18px outlined numerals — the verified legibility setup — only the material changed; the Doom badge stays deliberately loud). Code: `Card2D._build_chart_proto()` + `_chart_seal`/`_chart_rule`/`_chart_diamond`/`_kw_seals_rail`, routed from the live `USE_NEW_FRAME` branch; v6 stays selectable via `redesign_proto` for reference. Verified: 8-card spread harness (`tools/card_redesign_v6/render_v7.tscn`, incl. long-name / curse / no-art / spell stress cases) + `combat_probe` render (baked hand cards, field token, overlay numerals all seated). Found-the-hard-way note: the Card2D control is natively **225×300** — absolute offsets are real pixels; only `_center_at_point` speaks the 300×400 design space.

- 🃏📜 **2026-06-11 (latest): CARD FRAME v8 — "campaign writ" — LIVE, evolved in place from v7** (user verdict on v7: still "kind of sus"). The honest diagnosis: v7 got the furniture right but kept a flat near-black board, so the card read as **a dark void wearing thin gold jewelry** — floating tooltip-like text well (fixed-size: "Armored." drowned in an empty box), poker-chip stat discs, keyword pins occluding the PD-master art, dead bottom strip. v8 flips the material: **the card IS the campaign parchment** (the same paper world as the Sicily plate and tooltips), with the ink-and-gilt furniture printed ON it — the StS warm-frame read fused with the game's own chart identity. Moves: (1) parchment leaf with real tooth — new seeded **`ParchmentPlate`** painter (mottle washes, sizing lifts, foxing, fiber hairlines, edge toast that follows the rounded corner — a painter, NOT texture tiles, so nothing leaks past the leaf radius onto the background); (2) art plate grew 131→139px and gained an ink mat — the painting dominates and nothing is pinned over it; (3) stats are now real pressed **`WaxSeal`s** — seeded irregular blob silhouette per card+stat (every seal cools differently), cooled-rim line that doubles as polygon AA, stamped impression ring lit away from the top-left light, matte sheen, micro-nicks; same `_chart_seal` signature + 56px corner boxes, so the bake-overlay contract holds and the 38px field-token seals inherit the material for free; (4) the text well is GONE — rules text is written straight on the page inside a hairline ink box with metal corner ticks (chart-legend language), and the keyword gold `#e8b547` (tuned for dark wells, invisible on cream) is re-inked **`#7a4f10`** (~5.5:1) in the chart layout only; (5) keyword devices moved off the art into an ink-stamp row in the bottom margin (fills v7's dead strip, mirrors the spells' letterspaced footer — now letterpress ink, not gilt); (6) curse = a **corrupted document**: murk-green stains + heavier foxing on sickly paper under bone rules. Verified: 8-card stress spread re-rendered (long-name/curse/no-art/spell cases), token renders, `_probe_combat` full-scene screenshot (baked parchment hand pops off the dark battlefield; overlay cost numerals seated on the navy wax; threat-tinted enemy token numerals legible); max in-pool desc (99 chars) fits the text clip with a spare line.

- 🃏✒️ **2026-06-11 (latest+1): CARD FRAME v9 — "illuminated writ" — LIVE** (user: "full freedom to destroy everything if necessary"). Verdict: the writ material was right, but v8 still had three tells of UI-not-artifact, and v9 burns each one down. (1) **The silhouette was a mathematically perfect rounded rect** — the one thing real cut paper never has. New `WritLeaf` class: a seeded deckled leaf — fixed-count rounded-rect path, every vertex pushed inward by a smooth two-frequency wave — where the sheet fill, the hand-cut ink contour, AND the edge toast all draw along the SAME wobbled path (path-locked, so the organic edge never separates from its shading; wobble only pushes inward, so inset children always stay on paper). `ParchmentPlate` slimmed to interior-only ageing (clipped 3px). (2) **The nameplate was a flat dark bar** — the most "programmer art" element left. New `InkCartouche`: a swallowtail banner — ink field, metal edge, notched ribbon-tail ends, inner double-rule hairlines, settle shadow — the name moment is crafted furniture now, and names stay legible even tucked into the hand fan. (3) **The rules text was a modern rounded sans on period paper.** New `GameTheme.font_card_body` — **Alegreya** (OFL book serif designed for literature, 415KB variable TTF) at wght 500 body / 760 bold, sizes +1 vs the sans scale so the hairline serifs survive AA — scoped to CARDS ONLY (HUD/menus keep Nunito; the card page is diegetic paper, so it gets the diegetic book hand, old-style text figures and all). Garnish: the rarity gem is now a faceted diamond SET in an ink mount with a glint (reads at hand scale), and the art fillet wears metal plate-mark squares on its corners (museum mounting). Verified: full 8-card stress spread, 3× detail zooms (deckle, cartouche, wax seal), `_probe_combat` live scene (cartouche names readable in the tucked fan; cost seals seated; tokens unchanged-correct). Bake-overlay contract untouched.

- 🎬 **2026-06-11 (latest+2): CARD MOTION — hover-lift + wax-press landing** (user go on the v9 follow-ups). Two beats, both in Card2D so they live with the existing motion vocabulary (lunge/recoil/flare): (1) **Hover-lift** — the in-hand hover was an instant snap (`scale=1.15`, `y-80`, teleport); now a 0.11s cubic-out rise with a 0.14s gentle settle (picking up is eager, putting down is soft), plus a lazily-built **elevation shadow** (`_ensure_lift_shadow`: shadow-only StyleBox in a wrapper Control — Card2D is a PanelContainer, so a direct child's inset offsets get stomped by container layout) that fades in under the card as it leaves the table. Hover/drag tween-fight guards: `_start_drag` kills the lift tween (it writes scale/rotation directly), and the exit settle can't fight `set_hand_target`'s reflow tween (later-created wins per property). (2) **Wax-press landing** — `_play_landing_pop`'s outward scale pops (1.05→BACK settle for player flights, 1.35→rest for enemy drop-ins) replaced by the shared `play_wax_press`: quick squash (1.06x, 0.90y), a one-shot **gilt impression ring** radiating off the card edge (`_spawn_press_ring`, self-freeing), release with a small overshoot, then idle-bob re-arm — in the writ fiction, playing a card is pressing a seal, not inflating a balloon. Enemy drop-ins arrive at 1.22 and stamp through the same press. Verified by new `tools/screenshot/_probe_motion.tscn` (windowed): hover frame (card risen clear of the fan, shadow under), press frame (mid-squash), ring frame (gilt ring visible mid-expansion) — plus the probe's own combat boot runs every opening enemy placement through the new landing with zero script errors. MJ-painted cartouche flourishes remain open (needs the user's Midjourney pipeline per the event-art precedent).

- ⚔️🗺️ **2026-06-11 (latest+3): COMBAT SCENE VISUAL PASS — war table, threat legibility, HUD plaques** (user supplied a live screenshot and asked what to improve most; approved all three calls). (1) **The battlefield was a void** — 16 dark boxes floating in dark space. New `TablePlate` painter in Combat.gd dresses the substrate as the campaign table the war is run from: wood grain (the existing anisotropic tex), a faded chart of the theater inked straight on the surface (double-ruled coast + sea hatching, dashed march routes, site rings, compass, cup stains — seeded per encounter+act), scorch blotches along the clash band and edges that grow per act, a quiet bronze table fillet, and a **charred furrow** under the gilt clash seam. Lane tracks thinned (0.34→0.22 fill) so the table shows through; first ink pass was invisible under them — alphas were re-tuned ~1.8× off a render, not a guess. (2) **The threat flag drowned in warm fights** — infernal grade + red-lit art + bare red ring stacked hue on hue. `set_threat_flagged` now layers a **dark keyline under the crimson ring** (contrast separation, not more red), adds a **sword badge** at the card's top edge (shape read survives any grade; uses `tex_icon_sword`), halves the red shadow bloom, and lifts the pulse floor 0.35→0.55 (no more strobing). (3) **HUD furniture joins the writ fiction**: the encounter passive is now a posted **parchment edict** (writ paper, oxblood frame, dark Alegreya-bold ink — the standing rule reads as a notice, not a toast), "YOUR TURN" went gilt Cinzel like the title above it, and the INCOMING/wave chips warmed to the ink-board family with squarer plaque corners (their dynamic border colors — the functional voice — untouched; the faceted mana crystal was assessed and deliberately left: it's already a cut-gem diegetic build, not the old gloss ball). Verified: full-scene probes of `executioners_block` (act-3 warm grade, passive edict, 3 flagged enemies) and `goblin_scouts` (act-1 verdant, no passive → no edict) + zoom crops of the table surface, flagged card, and edict. **RESTRUCTURED same day (user: "honestly its still dark and empty")** — the first pass kept the 16 full-size dark socket wells, which covered the table and left the midscreen a grid of voids no matter what was painted beneath. The fix was structural, not cosmetic: (1) empty sockets became **`StationMark`s** — corner brackets + hairline + survey cross + a faint ownership wash drawn ON the chart (positions marked on the map, not holes carved into it; the slot's behavioral children — Highlight/ContactShadow/Cell — untouched); (2) the substrate lifted from near-black to **lamplit wood-brown** (0.05→0.118) with doubled wood grain + a fiber layer, restoring the Hearthstone value structure (bright table, dark room); (3) lane tracks thinned to 0.10 fill so the surface runs continuously. Re-verified both probe encounters: the board now reads as one warm bracket-gridded campaign table with creatures standing on it. **INSTRUMENT PASS (same day, user: "the mana and the health bar and the sides of the board look amateurish, like an unfinished building")** — the table got crafted and exposed the chrome as scaffolding. Fixes, all in the established kit: (1) **HP medallions** (both sides) — the 14px-radius saturated-fill lozenges with bright 3px rims (the loudest mobile-UI read on screen) became ink plaques with restrained metal rims (bronze player / oxblood foe) holding a matte **sealing-wax bar** (oxblood, faint cooled-sheen top line; the draining `Fill` node + `full_w` meta contract untouched, so the HP tween works unchanged); (2) **both portrait banners** — flat ColorRect backdrops + weak tinted nine-patch frames became ink-board plates with dark edges, soft seats, and a 1px metal fillet wrapping portrait + plaque (the card art-plate language; bronze vs oxblood mirrors the pair); (3) **mana** — the light-blue Nunito "MANA" debug-label caption became letterspaced dim-gilt Cinzel "M A N A" (the crystal itself stays — it's a deliberate faceted build); (4) **the right rail aligned** — the INCOMING chip widened from 146px to the full 236px column shared by the enemy plate and wave chip; three different widths was half the "unfinished building" read. Verified on both probe fights; the player banner happened to boot at 23/25 HP, proving the wax-bar drain renders correctly mid-fight. **Follow-up (user go):** the deck/discard piles became **writ-stacks** — parchment leaves in the v9 card material (the dark ornate card-back texture read as a different product from the writs fanned beside it): the deck's top leaf carries a small oxblood wax seal (orders still sealed), the discard's paper is cooler and bare (spent dispatches), captions went letterspaced Cinzel (D E C K / D I S C A R D), count badges unchanged; and the mana crystal got its **ink plinth** (bronze-ruled mount behind the gem's bottom tip) so the last floating instrument on the left rail reads as mounted. Probe-verified with zoom crops; the paper accents now triangulate across the screen (piles ↖ / edict ↑ / hand ↓).

- 📖 **2026-06-10 (later): COLLECTION becomes a full compendium** (render-verified all 3 tabs: `tools/screenshot/collection_{cards,relics,potions}.png`). The Card Gallery was the only browser in the game — ~130 relics and all potions were invisible outside a run. `Collection.gd` rebuilt with tabs: **CARDS** (the existing real-Card2D baked gallery, now guarded by a view-generation counter so switching tabs mid-bake aborts cleanly), **RELICS** (all 131 as readable plaques — run-HUD chip with tier halo + gilt name + effect text in the open, sectioned Starting/Combat/Utility/Boss, alphabetized), **POTIONS** (12, tinted bottle icons, same plaque). Plaque tile = dark-ink panel with a tier/potion-color left rule (the bark-scrim/map-tooltip language). `show_tab()` is public for probes; new probe `tools/screenshot/_probe_collection.tscn` (windowed) renders any tab. Main-menu button renamed CARD GALLERY → COLLECTION. Possible follow-ups: per-relic painted icons for the letter-fallback chips; an unlock/seen dimming layer if meta-progression ever wants it.

- 🛣️ **2026-06-12: THE ROAD TO THE KEEP — the between-fight walk is now the Inscryption cadence** (user: "events between fights are super boring… supposed to have 2-3 between each fight in all kinds"). Slice 1 (`32af432`, logged late) replaced the per-node probability table with a fixed **row skeleton** — fight rows alternate with wayside bands, fight count CONSTANT per act — plus escalation/pursuit. Slice 2 (this session) finishes the road:
  - **Two stops between every fight.** Map 10→12 rows (`FIGHT_ROWS [0,3,9]`, `ELITE_ROW 6`, `REST_ROW 10`, `BOSS_ROW 11`), acceptance window 16–23 sites/act; every between-fight gap is a PAIR of wayside rows dealt from event / roadside verb / recruit / shop / treasure. `_force_one` now eats a wayside halt when a guarantee row dealt no plain slot (a 1-wide spoils row can no longer lose its treasure — found by `_probe_map_balance`, seed 1337). Old saves stay loadable (navigation reads `map_data` generically; no SAVE_VERSION bump). Plate perf unchanged (probe: 2.2 ms/frame view-change, 0.00 px button drift); legend gained the WAYSIDE entry.
  - **WAYSIDE HALTS ARE LIVE** — slice 1 typed them at gen but routed them nowhere (clicking a wayside node did *nothing*). New `scenes/wayside.tscn` + `scripts/scenes/Wayside.gd`: four ONE-DECISION roadside verbs, dealt per act from a shuffled pool at gen (`node.wayside_id`), advertised on the chart tooltip (`MapView.WAYSIDE_TIPS`): **Drill Yard** (push-your-luck creature training — +1/+1 per pass, even odds each extra pass or the yard takes 3 HP and closes, banked passes kept), **The Quartermaster's Scales** (one trade by weight: sell a card for 15g + 12g/Command — removal AND coin; or buy a potion/meal, dead trades rendered disabled with the reason), **The Standard-Bearer** (lift a transferable keyword off one creature and pin it on another — Inscryption sigil-transfer; applied atomically at the final pick; whitelist excludes cost/data-coupled keywords), **Supply Cache** (1 of 3 rolled spoils — gold/potion/heal/next-fight Levy Spearman/+1 Command/random common; dead rewards filtered before the roll). Plumbing: `RunState.current_wayside_id` through visit/save/load; wayside upgrade paths (`drill`/`grant_kw`/`strip_kw` via `apply_wayside_upgrade`) ride the one-entry-per-card upgrade slot, so a drilled creature can't also be forged. Probe: `tools/_probe_wayside.gd` (skeleton invariants × 5 seeds × 3 acts, upgrade-path math, verb save round-trip) ALL PASS; all four screens + the drill push-luck beat render-verified (`tools/screenshot/wayside_*.png`).
  - **Event pool: two new FORMATS + a bridge variant + art stand-ins.** New modal `transform_choice` → **The Chrysalis Fence** (the StS transform — the deck-sculpting verb the pool lacked: not removal, not upgrade, CHANGE; curses excluded, starters hatch as commons). New modal `dice_run` → **The Bone Pit** (a *looping* push-your-luck knucklebones game, not a one-shot wager: pot opens at 25g, grows 18–34g at 2-in-3 per cast, busts 1-in-3, bank any time — dead legionaries, jaw-click applause). **The Ferryman** (`at_bridge`-gated) so repeat river crossings aren't always The Crossing. Events now accept an `"art"` stand-in field (bespoke `assets/events/<id>.png` still auto-wins the moment it lands — the Midjourney pipeline upgrade path); The Crossing was rendering ARTLESS and now borrows the tollkeeper plate. Render-verified: chrysalis + bone pit screens, dice mid-run, transform picker.
  - **THE OLD-EVENT REMAKE (same day; user: "the structure of the old ones are all too much the same — just remake the old ones").** Three reusable interaction engines added to Event.gd, then ~10 of the flattest prose-and-three-trades events converted onto them (ids/art/gates kept, fiction kept, the *play* rebuilt): **`risk_loop`** — the generic "do it again?" push-your-luck spine, bust mode (success pays immediately and advances; failure ends with a penalty — per-step odds printed on the button) + jackpot mode (every press costs, the roll decides when the payoff modal opens), with a non-lethal HP guard that withdraws the action when the player can't pay → Thrice-Blessed Spring (sip by sip), Gravesong Choir (verse by verse), Weeping Orchard (fruit by fruit), Woodcutter (-2 HP a swing until the trick clicks → upgrade picker). **`roll_table`** — committed-action mystery outcomes (weighted, receipts inline) → both Fork roads, striking the Drowned Bell, forcing The Crossing's bridge. **`pawn_appraisal`** — the haggling counter: she pulls a RANDOM card from your deck and names a price (rarity+cost), "show her another" cools her offers 15% (3 shows max) → Pawnbroker's Window. **`dice_run` parameterized** (stake / add vs double mode / bust odds / {pot}-{gain} text tokens) → Coin That Won't Land is now a true double-or-nothing ladder (stake 25, pot 40, even odds per call). Rotting Carnival flipped to the hidden-tell format (three ??? tents, the sounds at the flaps are the only intel). Verified: parse clean, both regression probes ALL PASS, and a driving probe (`_probe_event_remake.tscn`) that plays the engines — spring mid-loop, woodcutter through to the upgrade-picker handoff, both appraisal offers, the coin stake deduction, a fork mystery resolution — all render-checked.

- 🧪 **2026-06-18: STABILITY + BALANCE SWEEP (autonomous; "playtest → patterns → improve").** Five new headless probes; build confirmed shippable; **4 balance fixes + 4 card nerfs + 7 softlock fixes** landed. (Mid-session the user asked to fan out **parallel sub-agents**; key constraint found: **sub-agents' shell is inconsistently denied** — only the main session reliably runs the Godot binary, so agents serve as static analysts/proposers and the main session drives + certifies every Godot run. Agent 1 was fully blocked but returned the encounter inventory; Agent 2 landed 4 verified card nerfs; Agent 3 was blocked but statically found 7 real softlocks + wrote the probe to prove them.)
  - **Events now verified crash-clean** (`tools/_probe_events.gd`) — the #1 documented unprobed crash surface. Walks all **32 events** (doc "~41" is stale) × every choice/follow_up/blue-option/roll_table-outcome/risk_loop-step+bust under two RunState fixtures: **338 `_apply_effect` calls + 34 gate checks, 0 crashes.** (Combat re-confirmed too: 50 fights, 0 crashes/softlocks.)
  - **Content cross-ref audit — no dead content** (`tools/_probe_audit.gd`, `_probe_cardmap.gd`): every spell type / 22 keywords / 20 creature passives / on_enter+on_death types / 30 encounter passives / 5 reactive keys / all 135 relic ids are wired to a handler (relics dispatch by **id** via `_has_relic`, not the `effect` field; the 6 "unreferenced" boss relics enforce their downsides by the `downside` string). Every draftable card has a hand-crafted UPGRADES entry; copy clean. **Floop confirmed fully vestigial** — zero cards carry a `"floop"` field, so the entire toggle path (UI/badge/tutorial/`can_attack`/net snapshot) is dead code that can never fire; left in place (user previously chose board-verbs over restoring floop) but flagged as the biggest available combat-variety lever.
  - **Balance telemetry harness** (`tools/_probe_balance.gd`, `--only=`/`--prefix=` filters): every encounter × 5 heroes on **bare starter decks**, win-rate/rounds/HP per encounter. Read **within-tier relative** comparisons (the bare-deck flood-bot floor skews boss numbers low). **Finding + FIX:** the five act-3 finale **amalgams** should be peer-brutal but `amalgam_stalwart` was beatable **4/5 by the mindless flood-bot** vs siblings **0–1/5** — its deck was a low-ATK armored turtle with no face clock. Swapped `passive_id` `throne_husks`→**`formation_drill`** (the Last Wall's own engine — and `throne_husks` didn't match its identity; `amalgam_raider` already carried the Stalwart's drill). **Re-probe verified: 4/5 → 0/5, into peer band, no overshoot.** Rival-lord (`rival_*`) relative sweep also run — tight cluster 0–2/5, **no outlier** (confirms the amalgam was isolated, not systemic). Act-1/finale bosses read 0/5 but that's the bare-deck floor (a progressed real deck differs), not actionable from this probe alone. Future: a smarter auto-pilot (targeted spells, positioning, draft picks) would turn the floor-bot's numbers into real difficulty curves.
  - **Full per-tier sweep (all acts) → 3 more within-tier outlier fixes (all re-probe-verified):** `hollow_king` (a2 boss) was the easy-boss outlier 4/5 vs peers 0–2/5 (its anti-tall snipe does nothing to a wide board) → Hollow Knights 2/4→3/4 + face 29→31 → **1/5**. `void_walker` (a3 elite) 5/5 (its deck-mill/discard threat is wasted on a fast deck) → reinforcement 2/2-swift→4/3-swift-piercing + face 27→29 → **3/5**. `corrupted_shepherd` (a3 elite) 5/5 → Bone-Rams 4/3→5/4, face 27→31, reinforcement→4/3 → **4/5 but bot HP-on-win 15.8→11.5**; held at 4/5 because it's a *death-punish* fight (`wolf_pack_revenge`) that the mass-trading flood-bot is the wrong oracle for — kept the moderate buff (real pressure up) rather than overtune for real decks. `puppeteer`/`glass_menagerie` (4/5) left as borderline. Normals all healthy (2–5/5, no trivial outliers).
  - **Card-pool nerfs (`CardDB.gd`, Agent 2, parse+load+autorun verified):** 4 clear over-curve outliers trimmed — `glass_knight` (triple-keyword 2-drop) 3/2→3/1, `ember_stalker` 3/4→3/3, `cleave_hound` 3/3→2/3, `crystal_sentry` (double-shield common) 2/3→2/2. UPGRADES deltas intact; starters/hero-cores untouched.
  - **Non-combat softlock fixes (`Event.gd`×6 + `Wayside.gd`, Agent 3, flow-probe certified):** closed an empty-deck crash class — 5 single-pick removal/sacrifice/sell handlers lacked the `deck.size()<=1` floor the multi-removal/shop paths already enforce (remove to 1 card → event picker → empty deck → crash on next combat draw); plus `_start_butcher_mode`/`_start_mirror_twin_mode` lacked the empty-eligible guard their siblings have (creature-less deck → no-exit blank picker). New `tools/_probe_flow.gd` drives every modal picker + wayside/recruit/reward/shop/rest/treasure under 5 fixtures incl. `one_card`/`all_curse`: **145 drivers, 0 invariant fails, 0 errors.** Main-session audit also confirmed all `remove_card_at` callers + all reward/relic-roll `[0]`/`randi()%` sites are guarded (no pool-exhaustion crash).
  - **Targeted spells certified (`tools/_probe_spells.gd`):** the autorun flood-bot never casts targeted spells, so every `enemy_creature`/`friendly_creature`/`any_creature`/`any` resolver was unexercised. New probe stands up friendly+enemy targets and calls `_resolve_spell(data, _auto_target_for(t), lane)` for each — **all 22 targeted spells resolved, 0 crashes.** Combined-diff cert: autorun re-run = **50 fights, 0 crashes/softlocks**; parse-check clean. **Net for the session: combat, events, modal flows, AND targeted spells are all now crash-verified.** (NB: a large pre-existing uncommitted working tree — ~1480 lines Combat.gd + the 06-17 card pass — sits under all this; left for the user to commit.)

- 🤖 **2026-06-18 (later): SMARTER BALANCE BOT + modal-combat coverage + one passive-inversion fix** (continuation of the playtest→fix→verify loop; all probe-verified):
  - **`_probe_balance.gd` bot upgraded from flood to *competent*.** The old bot dropped every **targeted** spell unplayed — so the Stalwart/Acolyte/Pyromancer starter decks were silently throwing away their **Strike** removal, making their floor artificially low. The bot now casts targeted damage/buff spells via the real `_resolve_spell`+`_after_spell` path with **basic target priority** (offensive → the highest-ATK enemy the spell can kill, else the biggest threat; buffs → the player's biggest attacker; holds removal on an empty board since the hand persists). Verified live: **139 targeted casts** across a 50-fight slice, parse-clean, no crashes. A new report line tallies casts so the path can't silently regress to a no-op. **This turns the bare-deck "floor" into a real competence curve** — exactly the smarter-auto-pilot follow-up the prior entry called for.
  - **What the smarter floor revealed (2 independent 5-hero samples = 10 games/enc):** a3 elites split hard — `void_walker` **10/10** and `corrupted_shepherd` **9/10** fold to competent removal (the prior session's anti-flood-bot buffs were masking this), while `glass_menagerie` stays **3/10** (it's an explicit *race* vs `executioner_face` glass-cannons — intended brutality). a2 `puppeteer` **9/10**, a2 boss `hollow_king` **6/10**. **Deliberately NOT re-tuned** — chasing these off a bare-deck floor would just thrash last session's calls; logged as a finding instead (the real read is "void_walker/corrupted_shepherd are soft under competent play," not "glass_menagerie too hard"). **Normal fights swept clean under the smart bot** (the caveat-free signal — normals *are* faced with near-starter decks): all 10 normals 4–5/5, HP-on-win 11–19, none trivial-in-1-round, none impossible → no normal-fight tuning warranted.
  - **One fix — a genuine tier inversion (not a win-rate chase): `rival_stalwart` (the ACT-1 boss) was wearing the act-3 finale's passive.** `formation_drill` (both-row, round-2, compounding +1/+1 — the snowball that the prior session deliberately made *amalgam_stalwart* 4/5→0/5 with) is otherwise used only by the hp-40 act-3 ascendants/amalgams. The act-1 gate carried the full act-3 engine, making it the hardest boss in the game (0/10 vs the a2 boss's 6/10). Added a gentler **`formation_muster`** variant (`EncounterEffects.formation_drill_tick(..., front_only=true)`: front rank only, from round 3) and repointed rival_stalwart at it; **the shared `formation_drill`/`formation_lockstep` act-3 tick is untouched** (re-probe of amalgam_stalwart confirms it's unchanged at 1/5). Bare-bot win-rate stays 0/5 (the Last Wall's armored-wall identity dominates the bare-deck floor — "Stone does not argue"), but avg rounds 5.0→5.8 confirms a real softening; the value is for *developed* decks, where the act-1 gate is no longer the game's harshest snowball. Verified on all 3 tick branches (front_only / both-row / lockstep), 0 errors.
  - **COMPLETE 50-encounter smart-bot sweep (run as 5 parallel slices) + the key insight it produced.** Full per-tier competence curve. **All normals (a1/a2/a3) healthy 3–5/5.** Within-tier outliers and what they mean: the "too-easy" ones — `puppeteer` 5/5 (anti-summon), `void_walker`/`corrupted_shepherd` 5/5 (deck-mill / death-punish), `rival_pyromancer`'s void_exile — are **all conditional *punish* mechanics the bot sidesteps by construction**, so the bot *overstates* their ease vs a real human who triggers the punishes (the handoff's "bot is the wrong oracle for disruption fights," now confirmed game-wide). The genuinely *hard* ones (`glass_menagerie` 2-3/10, `rival_raider` 0/5 @ **3.0 rounds**) are all **unconditional pressure** — appropriately hard. **Conclusion: the curve is healthy; buffing the punish fights off the bot's number would over-punish real players, so they were left alone** (watch-items only: `the_black_tide` 4/5 = easiest a3 boss; `amalgam_stalwart` 3/5 = softest finale, but punish-adjacent + complex phase structure + already tuned last session).
  - **The one tune that survives the oracle filter — `rival_pyromancer` (act-1 boss).** Its punish (exile 1 card/turn) is the weakest in the game (irrelevant in a ~7-round fight short of decking out), so its **5/5 vs every rival sibling at 0–2/5** was a genuine raw-durability gap: at **21 HP it was the lowest-HP rival** (siblings 22–23) and the bot simply burned it down before its slow ranged board mattered. Hardened to a proper threat: HP **21→28**, Burning Glass **3/2→4/2**, Cold Lector **3/4→4/4**. **Re-probe: 5/5 @ 8HP → 3/5 @ 4.3HP** — top of the peer band, wins now bloody, no overshoot.
  - **Second tune — `rival_raider` was an un-counterable 3-round alpha-strike.** At **3.0 avg rounds it killed the bot in half the time of every sibling rival** (stalwart 5.8 / acolyte 5.4 / kindler 5.8 / pyromancer 7.8) — below the threshold where its own "wall all four gates" counterplay can be set up, since its on_enter face-chips fire regardless of lanes (the un-counterable part of the burst). Softened the alpha without touching Swift/Overrun/the harpy_swift_face lane-toll: Salt Harpy **3/2→2/2**, First Ashore on_enter face **2→1**. **Re-probe: 0/5 @ 3.0rd → 2/5 @ 4.0–6.6rd** — counterplay window decisively open, in peer band. **Net: the act-1 rival tier went from a broken 0–5 spread (a 3-round alpha + a 5/5 pushover) to a tight, healthy 0–3** (stalwart 0 · acolyte 0–1 · raider 2 · kindler 2 · pyromancer 3), verified in an independent combined 5-hero cert. (Aside: `dragon_lord`/`iron_warden`, the legacy a1 bosses reading 0/5, are confirmed **never selected** — `_boss_encounter_for_act` always returns the authored `rival_<hero>` kit — so they're vestigial fallbacks, left as-is.)
  - **Git + doc hygiene:** `build/` + `.export_templates/` added to `.gitignore` (machine-local build artifacts that were showing as untracked). CLAUDE.md/memory counts re-verified against live code and synced: **162 cards · ~135 relics · 26 keywords · 50 encounters · 32 events** (were 154/~130/24/41/—; content grew since the last audit + the doc's KeywordEffects header already said 26).
  - **Combined-diff crash re-cert:** full `_probe_autorun` re-run over the EncounterEffects/EncounterDB changes = **50 fights, 0 softlocks, 0 SCRIPT ERRORs** (28/22 W/L on the rotating stress decks); parse-clean. Combat stays crash-verified.
  - **More reusable probes + a developed-deck lens.** `tools/_probe_boardverbs.gd` — first probe to ASSERT correct *behavior* (not just no-crash) of the 4×4 position verbs (shove/hook/vanguard/snipe): **10/10 board-state assertions pass.** `tools/_probe_curve.gd` — audits every draftable creature vs the stat-for-cost curve (doom/wither treated as downsides; walls bucketed): **pool healthy, no trims needed.** `_probe_balance --draft` — gives every hero a generically-strong package so the BOSS tier reads instead of bottoming out at the bare-deck floor. **Findings:** act-3 amalgam finales 0–3/5 developed = correctly brutal ✓; act-1 rivals developed = pyromancer 5/5 + raider 1/5 (the two the bot can play around, healthy) but acolyte/kindler/stalwart **0/5** — and those three are exactly the punish/wall bosses the bot mis-plays, so `--draft` *separated deck-strength from bot-skill* and isolated their hardness as **bot oracle-bias, NOT confirmed overtuning → flag for a human playtest, do not tune off it.** Also synced rival_stalwart's BOSS_PHASES[0] doc field (was the stale `formation_drill` after the passive→`formation_muster` change; phases[0] only documents the start, so dead-but-misleading — now coherent). **Final full-suite regression gate: autorun 50/0 · events 338 · flow 145 · spells 22 · modal 9/9 · curve 67 · boardverbs 10/10 — ALL GREEN.**
  - **The "968-file / 913-deletion" working tree — INVESTIGATED, it's benign.** The 913 "deletions" are the **source half of an intentional, documented asset-shelving** (`assets/_unused/_MOVED_MANIFEST.txt`, dated 2026-06-17, with restore steps): all four `painterly-N` spell-icon staging packs (882 files) + orphan content art + `frames/_backup_v3`/`sources` were *moved* to `assets/_unused/` (Godot-ignored via `.gdignore`). Git sees old-path-deleted + new-path-untracked. **Nothing in code loads any of it** — `CardArtAliases.try_load_spell_art` reads `assets/spells/<card_id>.png` (top level; all final per-card art intact). The rest of the tree is a multi-session sprawl never committed: the **entire `tools/_probe_*` toolkit**, the **multiplayer skirmish feature** (`scripts/net/`, `scenes/net_*`), the conquest/06-17-card code (8 modified scripts), new event art, and **`build/`+`.export_templates/` (should be gitignored, not committed)**. Recommended commit strategy when the user is ready: logical chunks (probes / balance fixes / asset-shelve-as-move / multiplayer), gitignore the build artifacts — NOT one blob. **Floop: user reaffirmed "leave it retired" (2026-06-18).**
  - **Modal-card combat coverage closed (`tools/_probe_modal.gd`, NEW).** The autorun/balance/spell bots all skip cards whose on-play opens a picker (Discover/Choose-keyword/Copy) — they `await` a click. The new probe injects each into hand (via the game's own `_add_discovered_card_to_hand`), plays it, and **auto-drives any button-overlay by emitting the first option's `pressed`** (the discover/keyword/recycle pickers are all z-index-200 ColorRect overlays with `pressed`-lambda buttons — uniform to drive). **All 9 covered resolvers ran clean** (familiar/scholar/treasure_hunter/lost_tome/war_council Discover, adaptable keyword-choice, chaos_imp cast-random, copycat copy-friendly, doppelganger copy-last-dead) — the last unexercised combat surface. copycat is driven via single-candidate auto-resolve; doppelganger reads the death log directly; both + chaos_imp legitimately open no picker.

- 🔁 **2026-06-19: CONTINUATION SWEEP (autonomous; "finish this — aggressive token use, many agents").** Re-certified the build and landed targeted fixes; a 4-agent parallel static audit (combat-pipeline / data-integrity / copy-style / scene-flow) confirmed the playtest→fix→verify loop has **CONVERGED** — most agent findings were non-issues on verification, and I read the code behind each before acting. **Full probe suite GREEN:** autorun 50 fights / 0 softlocks / 0 SCRIPT ERROR (re-cert after the swift change), events 33/362, flow 145/0, spells 22/0, modal 9/9, relics 4/0, wayside PASS, audit fully-wired, curve healthy. Parse-clean throughout.
  - **Fixes landed (7, all parse+probe-verified):** (1) **`Card2D.play_wax_press`/`_spawn_press_ring`** guarded with `is_inside_tree()` not `get_tree()==null` — the latter *emits* a Godot `data.tree is null` error when a card lands detached (surfaced by `_probe_boardverbs`); identical behavior, no error spam. (2–4) **Copy-style §2/§3 on the runtime hover-inspector** (`Card2D._describe_trigger`/`_build_detail`): every "Deal N to X" → "Deal N **damage** to…", "enemy hero" → "enemy face", "all friendlies … HP", signed `-N ATK` debuffs; the on-enter line no longer **double-labels** ("On Enter: " was prepended onto a desc already starting "On-Enter:"); "On Death:" → "On-Death:". (5) **6 card descs** (Pikeman/Harpy/Raven + their "+" upgrades) "deal N to it" → "deal N **damage** to it". Broad `mana`-in-player-text sweep: **clean** (Command rename holds; every literal "mana" is a code identifier). (6) **`amalgam_stalwart` BOSS_PHASES[0]** synced `throne_husks` → `formation_drill` — coherence twin of the prior `rival_stalwart` phases[0] fix; **phase 0 is never applied** (`new_phase=i+1`, so crossing threshold[0] applies phases[1]; the start passive comes from the encounter `passive_id`), so **zero gameplay change**. (7) **Swift attack path** now applies the same `marked`(+2)/`enrage_vulnerable`(+1) vulnerability bonus the column path does (consistency; no-op unless an enemy Swift attacker hits a marked player creature).
  - **Verified NON-issues (declined after reading the code — agents over-flagged):** the "Hydra ignores Thorns" claim is wrong — `_resolve_hydra_attack` already takes a deliberate flat `take_damage(1)` per-bite recoil; adding `_apply_thorns` would double-count. The combat agent's only **HIGH** — "mid-combat on-death tokens lack summoning sickness" — is a **design call, not a defect:** this game has *no* summoning sickness (placed creatures attack the same round; the combat loop snapshots board state at start with no placed-this-turn gate), so a summoned token swinging is consistent with the existing model, and changing it would shift the tuned balance → **left as-is, flagged for the user.** `become_copy` base-vs-effective-atk is dead floop code (0 floop cards). The MapView picker/click race is blocked by `show_option_picker`'s full-rect `MOUSE_FILTER_STOP` scrim. Data audit found **0 value-vs-text contradictions** (only cosmetic dead `on_play_value` upgrade keys remain).
  - **Balance:** amalgam finales 0/0/**4**/0/2 — `amalgam_pyromancer` is the soft outlier, but its exile/keyword-theft is exactly the conditional-disruption the bot sidesteps and over-rates as easy (oracle bias), so **held as a watch-item, not tuned** (per the documented anti-overtuning discipline). No balance changes made this session.
  - **⚠️ Save-slot caveat (widens the handoff's slot-0 warning):** `_probe_wayside` (`visit_node`) and `_probe_flow`'s rest path (`_do_heal`→`register_rest_visit`) both reach `save_run` with `active_slot -1 → 0` = they **write save slot 0**. The handoff only named visit_node/end_run/map_live — add these two; avoid re-running them, or have them target a test slot. (autorun is disk-safe — uses `start_new_run`.)
  - **Still pending the user:** the large uncommitted working tree (40 commits ahead + this session's edits) awaits a commit decision — **nothing committed**; the summoning-sickness design call; the art follow-ups (plague_bell icon, non-PD portrait check).
  - **Deeper-coverage round (same session, "do more"):** certified the 3 not-yet-run probes — **conquest scaffold, skirmish, skirmish-combat, and the two-process loopback ENet `skirmish_net` (host+client) ALL PASS** → the full multiplayer stack is green (logic + combat + real transport). 3 more static agents (net/ subsystem, relic effect-vs-desc, encounter-passive correctness) found the code broadly faithful. **Fixes landed (parse + relics 4/4 + skirmish-combat + a fully-clean autorun re-verified):** `iron_buckler` per-ROUND → per-FIGHT latch (matched its "each fight" desc); `resonance_crystal` friendly-only (`not card.is_opponent` — was burning its once-per-fight latch on enemy deaths); 4 stale relic-name strings (Coffee Dripper/Fusion Hammer/Sozu → Hairshirt/Cracked Anvil/Temperance Vow); `iron_warden_siege` desc now documents its 3-dmg/round (vestigial boss); net `_net_update_creature` propagates keyword/max-HP to client nodes. **The autorun re-cert surfaced a real pre-existing use-after-free** — `GameTheme.show_deck_picker` awaited `host.get_tree()` on a host freed by a scene change (a `SCRIPT ERROR`, reachable in real play if a deck-picker is open during a transition); fixed to use the autoload's own `get_tree()`. The same pass cleared the `get_tree()`-on-detached error-spam class (`_die`, `_combat_vfx_target` [14×/run], 4 animation guards, play_wax_press) → **autorun now 0 SCRIPT ERROR / 0 `data.tree` errors / 0 softlocks.** **Flagged, not fixed:** `scroll_of_greed` + the shared `ON_PLAYER_DRAW` reactive gate on a stale `HAND_DRAW_PER_TURN(4)` threshold under the persistent-hand model — a deliberate balance pass, not a hasty edit on a shared lever. Memory corrected: reposition-over-the-wire is LIVE (only floop gated).

> This doc + `CONQUEST_REDESIGN.md` + `COPY_STYLE.md` are the **live source of truth** — trust them over CLAUDE.md / git history, which have drifted before.

---

## 0. Executive summary

**The game is much closer to shippable than its own docs suggest.** The audit found a feature-complete, ship-shaped build: full front-end (hero select, daily, custom seed, ascension 0–5, 3-slot **versioned saves with mid-run resume**, comprehensive settings, collection, credits), broad high-craft content (154 cards, ~130 relics, 41 encounters, 41 events, 5 heroes, 13 potions, 22 mutators — **no stubs**), and an unusually complete **audio system** and **save system**.

The true blockers are small and mostly hygiene. The real distance to a *polished* 1.0 is **breadth and polish** — art coverage, audio breadth, onboarding, and legal compliance — plus, optionally, the **Successor Wars redesign** (a multi-week epic that is the game's differentiator but is **entirely unbuilt**, sitting at pre-Phase-0).

### The central strategic fork (decide this first)
Because the current build is nearly shippable on its existing Slay-the-Spire structure, you have a real choice the audit makes informed:

- **Path A — Ship 1.0 on the current structure, redesign later.** Fix blockers → polish breadth → onboarding → launch. The conquest redesign becomes a marquee **1.5 / 2.0 update**. *Lowest risk, fastest to market, validates the game and funds the redesign.*
- **Path B — Build the redesign into 1.0.** Don't launch until the conquest map ships. *Launches with the differentiated vision, but adds months and the full faction-balance cost up front.*
- **Path C — Hybrid.** Ship a **demo / Early Access** on the current structure now; build the redesign during EA with player feedback.

**Recommendation:** do **M0 (ship-hygiene) immediately regardless of path** — those fixes are required for any public build. Then pick A/B/C. My lean is **C, trending A**: the game is good *now*; the redesign is worth doing but shouldn't gate a first public showing.

---

## 1. Current-state snapshot (corrected, grounded)

> Multiple source docs are **stale** — CLAUDE.md, README.md, and ASSET_REFERENCE.md all undercount. Trust this table (from the live code) over them.

| Domain | Live count / state | Ship-readiness |
|---|---|---|
| **Cards** | 154 in `CARD_POOL` (~95 creature / ~57 spell) + 18 enemy-only; every id has a hand-crafted "+" UPGRADE | ~80% — strong; weak 20% is spell-cluster bloat + vanilla bodies |
| **Relics** | ~130 (9 starting / ~85 combat / 8 utility / ~21 boss); **zero orphaned** (all `_has_relic` sites resolve) | ~95% — strongest area |
| **Keywords** | 24 defined, 21 fully dispatched; `structure` is a stub; `slay`/`adjacent` are inline/doc | ~92% |
| **Encounters** | 41 (A1 13 / A2 13 / A3 15 → 25 combat / 8 elite / 8 boss); 7 multi-phase bosses; deck-variants for replay | High craft; **no faction tags / no rival-lord kits** |
| **Events** | 41 (9 state-gated, recurring NPC chains, multi-stage branching) | High craft, no stubs |
| **Heroes / Potions / Mutators** | 5 / 13 / 22 — all complete | Done (the 5 heroes *are* the redesign's claimant roster) |
| **Creature art** | 90 PNG portraits + ~210-entry alias map; only 3 true gaps | Effective coverage high; leans hard on aliasing |
| **Spell art** | 60 PNG (~98%); **no alias fallback** (new spells must ship art) | 1 gap (`plague_bell`) |
| **Event art** | 35 / 47 (~74%) via Midjourney pipeline | **Largest content-art gap** (~12 missing) |
| **Boss splashes** | 6 named of 16; 10 use generic `boss_act<N>.png` | Gap (10 named splashes) |
| **VFX** | **None as assets** — all code-driven (shake + CPUParticles) | Biggest *juice* gap |
| **Audio** | System ship-grade (crossfade, per-act pools, buses, sliders, focus-mute); 14 SFX (placeholder UI-pack), 16 music tracks | Plumbing done; **content/mix breadth** is the gap |
| **Systems** | Scene flow, hero select, ascension, daily/seed, 3-slot versioned saves+resume, settings, collection, mutators — **all done** | Feature-complete |
| **Onboarding** | 5 reactive in-combat tips; **no front-loaded How-to-Play / glossary** | Main UX hole |
| **Meta-progression** | runs/wins/losses/fastest + ascension unlock; **no card/relic/hero unlocks, no achievements** | Scope decision |
| **Platform/release** | minimal `project.godot`; **no icon, splash, logo, input map, export presets, Steam SDK** | Not started |
| **Legal/credits** | in-game Credits exists but has **CC-BY attribution gaps + a non-PD artist** | **Compliance blockers** |

---

## 2. Ship-blockers (must fix before ANY public build)
All are Small effort; several are *legal* and non-negotiable.

| # | Blocker | Fix | Effort | Source |
|---|---|---|---|---|
| B1 | **Debug `print` instrumentation in `Combat.gd`** (`[PACING]`/`[COMBAT]`, 6 sites + 2 vars: lines 136, 1016, 1285, 2105, 6214, 6232, 6365) — ungated, spams stdout every round/attack | delete or wrap in `const DEBUG_PACING := false` / `OS.is_debug_build()` | S | systems |
| B2 | **Uncredited CC-BY art = license violation** — painterly **spell icons (J.W. Bjerk / "eleazzaar", CC-BY 3.0)** ship with **no attribution anywhere** | add to in-game `Credits.gd` + `CREDITS.md` | S | legal |
| B3 | **CC-BY packs not in the in-game Credits screen** (the binding surface) — **game-icons.net** (Lorc/Delapouite/willdabeast/sbed) + **pbmojART** | add both to `Credits.gd` | S | legal |
| B4 | **Non-PD artist credited as public-domain** — **Beksiński** (d. 2005, copyrighted ~until 2075) listed among "PD masters" → copyright risk; other named artists need a PD check | remove/replace Beksiński art+credit; verify each PD claim | S–M | legal |
| B5 | **`scripts/_shot.gd`** temporary screenshot harness (self-labeled "remove before ship") | delete | S | systems |
| B6 | **3 cards render a blank rectangle** (`tallow_doll`, `the_glutton`, `plague_bell` — no art, no alias) + corrupt **`pikeman.png`** | source 3 PD arts + 1 replacement | S | art |
| B7 | **`README.md` describes the abandoned 3D prototype** (launch/GitHub embarrassment); `ASSET_REFERENCE.md` + CLAUDE.md counts stale | rewrite README; refresh/retire stale docs | S | legal/systems |
| B8 | **Font credit drift** — Credits lists "Pirata One"; build uses **Lilita One** (uncredited) | fix `Credits.gd` / `CREDITS.md` | S | legal |

**M0 = all of B1–B8.** A few days of work; unblocks any demo/EA/store build.

---

## 3. Workstreams to a polished 1.0
Each: current state → definition of done → gaps → effort. (These are the "1.0 without the redesign" body of work; the redesign is §3-H.)

### 3-A. Card & mechanics content
- **State:** 154 cards, ~130 relics (near-complete), 24 keywords (21 live).
- **DoD:** no duplicative dead-weight cards; every keyword either implemented or removed; counts documented.
- **Gaps:** consolidate/differentiate the **board-wipe cluster (6)** and **mass-ATK-buff cluster (~5)** and redundant face-burns; give ~8 vanilla commons a small rider; **decide `structure`** (implement for boss charge-triggers or cut); cut/fix strictly-worse `scrap`; reconcile legacy `ENEMY_POOL` (18) vs EncounterDB into one tuned enemy source.
- **Effort:** M.

### 3-B. Run content (encounters & events)
- **State:** 41 encounters (high craft), 41 events (well-gated). Strongest design area.
- **DoD:** balanced boss distribution per act; events all art-backed (see 3-C).
- **Gaps:** **Act-1 boss thinness** (2 vs 3 elsewhere) — add 1; otherwise solid for the *current* structure. (Faction/rival authoring lives in 3-H.)
- **Effort:** S (current structure).

### 3-C. Art
- **State:** creatures effectively covered (heavy aliasing), spells ~98%, **events 74%**, boss splashes 6/16, **no VFX assets**.
- **DoD:** zero blank-art cards; events ~100%; named splashes for marquee bosses/elites; a baseline impact/cast/death VFX set.
- **Gaps:** **12 event arts** (Midjourney) [M] · **10 named boss/elite splashes** (PD masters) [M–L] · promote ~5 high-frequency alias-only creatures to own portraits [M] · prune `assets/frames/` dev cruft + `painterly-*/__MACOSX/` junk [S] · **VFX asset library** — the single biggest *perceived-quality* win and directly addresses the "combat feels samey → presentation/legibility" note [L].
- **Sourcing standard:** card/creature/boss = **AAA public-domain masters** (Doré/Goya/Velázquez/Schongauer/Vrubel/Fuseli), one at a time, rename cards to fit great art; events = **Midjourney** at `assets/events/<key>.png`; icons = game-icons.net.
- **Effort:** M–L.

### 3-D. Audio
- **State:** system is ship-grade; **14 SFX (placeholder UI-pack)**, 16 music tracks (per-act/node), full mixer + sliders + focus-mute.
- **DoD:** thematic combat SFX; ~40 cue coverage; ≥2 variants for repeated-scene music; a loudness + ducking pass.
- **Gaps:** swap placeholder combat SFX (hit/death/spell) for thematic sources [M] · add the **one dangling cue** `upgrade_confirm` + ~10 missing UI/keyword cues (error, hover, potion, relic, summon, sacrifice, Doom-tick) [M] · 2nd music variant for shop/map/event/rest (single-track fatigue) [M] · **music ducking** under stingers + loudness-normalize [M] · re-encode the large WAV tracks + 10 MB `fire.wav` to OGG [S].
- **Effort:** M (per-faction music is in 3-H).

### 3-E. Systems & UX
- **State:** feature-complete; saves robust; only reactive tutorial.
- **DoD:** a new player can learn the board/rules without trial-and-error; meta scope decided.
- **Gaps:** **front-loaded How-to-Play + always-available keyword/board glossary** (the 4×4 board, lanes, front/back, Swift pre-phase, win condition) — host it on the Collection screen [M] · **meta-progression scope decision** — ship ascension-only, or add card/relic/hero unlocks + achievements to `MetaState` [S–L, scope call].
- **Effort:** M.

### 3-F. Platform & release engineering
- **State:** not started — minimal `project.godot`.
- **DoD:** branded, exportable, controller-capable build with a store presence.
- **Gaps:** **app icon + boot splash + game logo/wordmark** and wire into `project.godot` [M] · **`export_presets.cfg`** (Windows + Linux/Deck) [M] · **InputMap + controller support + key rebinding** (required for Steam Deck Verified / "Full Controller Support"; game is mouse-only today) [L] · **Steamworks** (GodotSteam) for achievements + **Steam Cloud mapping** of `user://*.save` + overlay [L] · age-rating/content-descriptor prep (horror/gore/sacrifice themes) [S, external].
- **Note:** mid-run save/resume already exists (3-slot versioned) — only *Cloud* mapping is missing.
- **Effort:** L.

### 3-G. Legal & docs housekeeping (beyond the M0 blockers)
- **DoD:** every shipped asset correctly attributed; a consolidated license file; docs reflect reality.
- **Gaps:** ship a **`THIRD_PARTY_LICENSES.txt`** bundling CC-BY/OFL/CC0 credits [S] · finish PD-artist verification [M] · refresh **CLAUDE.md** (Combat.gd is ~11.8k not 4.5k lines; alias map is `CardArtAliases.gd` not `GameTheme.gd:1068`; relic/keyword/card/encounter/event counts) [S].
- **Effort:** S–M.

### 3-H. The Successor Wars redesign (the optional epic)
Full spec in [`CONQUEST_REDESIGN.md`](CONQUEST_REDESIGN.md). **Status: pre-Phase-0 — nothing built.** The 5-hero roster exists; everything else (faction tags, rival-lord bosses, amalgam, conquest map, recruitment decoupling, the 2 new keywords) does not.
- **Code (per the spec's phases):** RunState fields + `faction` tag + `get_ids_for` filter; shortened faction-tagged act map; rival-as-boss + finale routing; decoupled rewards + Recruit node; new keywords `overrun`/`formation` + `tithe`/`foresight` helpers (note: **`foresight`/scry half-exists** — Lookout scry, `look_top`, `frozen_eye`, Hexblade spell-scaling — so it's consolidation, not from-scratch).
- **Content authoring (the bulk):** **~13–17 new "hold" encounters** to make every faction fill every act — front-loading **Lanternhall (the worst-served: 0 Act-1 holds, no boss, only anti-spell mirrors) and Everflame**; **5 rival-lord boss kits + 5 pre-authored amalgam finales (~10 boss-tier defs)**; **~6–10 new Lanternhall creatures** (the one faction genuinely short on bodies).
- **Art:** **5 faction visual identities** (banner/tokens/legend) + **5 rival-lord portraits** + amalgam splash + conquest-map art + 2 keyword icons.
- **Audio:** re-tag music **per-faction** (currently per-act) + 5 faction themes + 5 boss themes + finale.
- **Balance:** the **~20-cell matchup matrix** (each hero playable *and* a boss vs. any other) — the real multi-week cost the spec's own §11 flags.
- **Effort:** **XL** (weeks–months). Ship via the spec's **MVP cut** first.

---

## 4. Milestones & sequencing

| Milestone | Contents | Gate / "done when" | Rough size |
|---|---|---|---|
| **M0 — Ship hygiene** | All blockers B1–B8 (§2) | no debug spam, legal-clean, no blank cards, README real | days |
| **M1 — Polish the game you have** | 3-A content polish, 3-C events+splashes+VFX baseline, 3-D audio breadth+mix, 3-E onboarding/glossary, 3-G docs | a stranger can learn and finish a satisfying run on the current structure | 3–6 wks |
| **M2 — Platform & store readiness** | 3-F (icon/splash/logo, export presets, controller), Steam page, capsule, screenshots, trailer | a wishlist-able store page + a controllable, exportable build | 3–6 wks (parallel w/ M1) |
| **M3 — Redesign vertical slice** | `CONQUEST_REDESIGN` MVP: faction tags, rival selection, shortened faction acts, rival-as-boss, decoupled rewards + 1 recruit node, 5 boss kits, pre-authored amalgam, map reskin | one full run through the new loop, end to end | 4–8 wks |
| **M4 — Redesign content-complete** | ~13–17 holds, Lanternhall creatures, faction art identities, per-faction music, finale art | every faction fills every act with its own art+music | 6–12 wks |
| **M5 — Beta & balance** | matchup matrix, ascension/economy tuning, QA pass, achievements | tuned, bug-pruned, content-locked | 4–8 wks |
| **1.0 Launch** | marketing beat, demo, day-1 build | — | — |

**Path A (ship first):** M0 → M1 → M2 → **launch 1.0**; M3–M5 become the **2.0 "Successor Wars" update**.
**Path B (redesign in 1.0):** M0 → (M1 trimmed) → M3 → M4 → M2 → M5 → launch.
**Path C (EA):** M0 → M1-lite + M2 → **EA launch** → M3–M5 in public beta.

---

## 5. Risks & dependencies
- **Faction balance is the dominant cost** of the redesign (the ~20-cell matrix + each hero as deck *and* boss). Mitigation: most cards/encounters exist; tag and re-skin before authoring net-new. Lanternhall is the known content hole.
- **Art is the long pole** for both 1.0 polish (events, splashes, VFX) and the redesign (5 identities + 5 lords). PD-master sourcing is one-at-a-time and slow; budget accordingly.
- **`Combat.gd` (11.8k lines)** is a maintainability risk that *compounds* as the redesign piles on — consider extracting the spell-resolver / floop / HUD seams **before** M3, or accept the debt.
- **Doc drift** has already misled (counts off by 2–3×). Refresh CLAUDE.md early (M0/M1) so future work is grounded.
- **Controller + Steamworks** are large, somewhat independent tracks — start early if Steam Deck Verified / achievements matter at launch.

---

## 6. Decision queue (what we need from you)
**Strategic:**
1. **Path A / B / C** — ship 1.0 on the current structure first, or build the redesign in, or Early Access? *(Reshapes the whole timeline.)* — **ANSWERED 2026-06-10: Path B.**
2. **Meta-progression scope** — ascension-only (ship as-is), or add unlocks + achievements?
3. **Platform targets** — Steam Deck Verified + controller + achievements at launch, or post-launch?

**From `CONQUEST_REDESIGN.md` §13 (only bite these when the redesign starts):** ending (true escape vs. eternal cycle); recruit currency (gold vs. renown); hold granularity; rival order (chosen vs. seeded); boss gate (N-holds vs. always-open-scaled); map shape v1 (reskin vs. territorial graph); lock faction names.

---

## 7. Immediate next actions (risk-free, start anytime)
1. **Do M0 now** — the blockers are all Small and several are legal. Biggest bang: B1 (delete debug prints), B2–B4+B8 (credits/license compliance), B6 (3 blank cards), B7 (README).
2. **Refresh CLAUDE.md** with the corrected inventory from §1 so every future task is grounded.
3. **Faction-mapping worksheet** (redesign Phase 0, pure data) — tag the 41 encounters; already ~80% mapped in the audit, just needs committing to the `faction` field.
4. **Pick the path (§6.1)** — everything downstream branches on it.

*Say the word and I'll convert any milestone into tracked tasks and start executing — M0 is the obvious first strike.*
